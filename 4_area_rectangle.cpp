#include <iostream>
using namespace std;
int main(){
    int length,width;
    cout<<"Enter length and width";
    cin>>length>>width;
    cout<<"Area of rectangle is "<<length*width;
    return 0;
}
